{
  "name": "argko",
  "private": true,
  "workspaces": [
    "frontend",
    "shared/db"
  ],
  "scripts": {
    "dev": "bun --filter frontend dev",
    "build": "bun --filter frontend build",
    "db:push": "cd shared/db && bun run db:push",
    "db:seed": "cd shared/db && bun run db:seed",
    "db:studio": "cd shared/db && bun run db:studio"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.10.5"
  }
}
